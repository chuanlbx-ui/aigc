{
  "name": "video-mixer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
